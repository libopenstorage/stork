apiVersion: stork.libopenstorage.org/v1alpha1
kind: Rule
metadata:
  name: mysql-backup-failing-rule
rules:
  - podSelector:
      app: mysql
    actions:
    - type: command
      value: invalid-command


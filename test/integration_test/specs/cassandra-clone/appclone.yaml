apiVersion: stork.libopenstorage.org/v1alpha1
kind: ApplicationClone
metadata:
  name: clone-cassandra
  namespace: kube-system
spec:
  sourceNamespace: cassandra-cassandra-clone
  destinationNamespace: cassandra-cassandra-clone-dest


apiVersion: stork.libopenstorage.org/v1alpha1
kind: ApplicationClone
metadata:
  name: cassandra-clone-rule
  namespace: kube-system
spec:
  sourceNamespace: cassandra-cassandra-clone-rule
  destinationNamespace: cassandra-cassandra-clone-rule-dest
  preExecRule: cassandra-rule

